<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.classapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClassApplication">
        <activity android:name=".adminExamActivites.deleteExamActivity"></activity>
        <activity android:name=".adminExamActivites.createExamActivity" />
        <activity android:name=".authuser.displayAllUsersActivity" />
        <activity android:name=".authuser.modifyActivity" />
        <activity android:name=".authuser.deleteUser" />
        <activity android:name=".authuser.adminActivity" />
        <activity android:name=".SignIn.SignUp" />
        <activity android:name=".SignIn.loginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>